---
include_vars:
  - include_vars.yml

includes:
  - abstract_test.yml

vars:
  customer_name: nkiraly

kubernetes:
  # all hosts in this group get variable indicating they are a kubernetes cluster node
  vars:
    kubernetes_cluster_node: True

kubernetes_master:
  # all hosts in kubernetes_master group get these variables
  include_vars:
    - kubernetes_master.yml
  # can define hosts in host group blocks with _aig_type host
  "10.20.1.8":
    _aig_type: host
    vars:
      master_10_20_1_8_only_var: tracey
